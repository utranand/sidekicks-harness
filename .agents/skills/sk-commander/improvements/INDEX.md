# Improvements — sk-commander

One line per artifact: id — status — hook. Newest first.

**Verification round 2 (2026-08-18):** the four artifacts corrected after round 1 (009, 010, 011, 016)
were each put through 3 independent perspective-diverse refuters at verify_tier=high. **All four failed** —
009/010/016 at 0/3, 011 at 1/3. Each carries its `rejection_reason` in full, including the salvage path its
refuters converged on. Rejections are recorded, not deleted: the diagnoses are mostly sound and the evidence
is worth keeping; it is the remedies that did not survive.

- 027-description-under-triggers-on-step-range-and-build-all-services — proposed 2026-08-28 — MEASURED: the shipped description scores 14/20 twice on the committed set; a hand-authored candidate scores 16/20 twice, flipping exactly "execute steps 1-5 ... fan stage 2 out" (0/3 -> 2/3) and "build all three services at once from this numbered step list" (0/3 -> 3/3) with no negative regressing. The concrete phrasings live only in the body's Trigger guidance, which the host never sees at selection time
- 026-catch-block-halt-omits-the-stage-the-contract-promises — PARKED 2026-08-28 (0/2 verified — anchors correct but no consumer reads `result.stage`, the catch block is OUTSIDE the keep-verbatim region so the attribution was wrong, and the proposed derivation mixes label-vs-index and yields nothing on the precondition path. A +1-line doc clause is the only remedy worth taking; see applied_notes) — SKILL.md promises `{ status: 'halted', stage, reason, summary }` on "any unrecoverable failure (including a missing precondition caught by `try/catch`)" and step 6 tells the runner to read `stage`, but the template's catch block (L568) returns no `stage` key — and that path is where a precondition failure and an `event-sidecar-diverged` throw both land
- 025-eval-set-positives-that-name-a-file-path-are-structural-zeros — PARKED 2026-08-28 (1/3 verified — OBSERVATION REFUTED: positive #2 names an equally absent path and scored 3/3, so path-bearing is not a structural zero; also single unreplicated run vs a recorded +/-4 variance. Do not apply as written; see applied_notes) — the first measured baseline is 14/20, and 4 of the 6 failures name a file path the scorer's NEUTRAL trigger-root cannot contain, so the model reads the missing path first and the scorer records no-trigger: a permanent 20-point floor that caps the set at 16/20 regardless of description quality. Rewrite those four to carry the intent without a path
- 024-resolve-anchor-does-not-tolerate-backslash-inputs-it-claims-to — proposed 2026-08-28 — the helper's header promises it "tolerates `/` and `\` inputs" and `isLeadingDot` accepts `..\…`, but `resolveAnchor` resolves with plain `path.resolve`, so on POSIX `..\sib` becomes `…/bundle/..\sib` — non-empty, exit 0, so step 3a's emptiness guard cannot see it and the corrupt anchor is baked into the Workflow script
- 023-skill-step-brief-tells-the-agent-to-pass-work-dir-omit — applied 2026-08-28 (VERSION.json 1.3.0->1.4.0; 3/3 verified, floor clear; risk in practice low) — template L326 renders `Pass work_dir=(omit) … so it anchors there` for a step with no work_dir, while its own bullet says "none — use the skill's normal scope resolution" and `docsSuffix`/`workItemSuffix` (L314-315) already have the correct conditional form; Fallback L455-457 is a third variant
- 022-fallback-worktree-addendum-lost-the-cli-branch — PARKED 2026-08-28 (1/3 verified — cosmetic, and subsumed by pending 013/014 whose fix deletes the block this would grow; fold into 013) — template L248 branches the commit message on CLI-vs-skill; the Fallback's hand-copied addendum (SKILL.md L485-493) hardcodes `commander: <skill> @ <work_dir>`, and a CLI step can legitimately be a cross-scope wave member
- 021-cli-step-output-capture-differs-across-the-three-copies — applied 2026-08-28 (VERSION.json 1.3.0->1.4.0; 3/3 verified, floor clear; alternative direction, 2:1) — SKILL.md L225 says the template captures the stdout/stderr tail, the template (L300-304 + the STEP schema) asks for it only on failure, and the Fallback (L478-482) puts it in the handoff on success: three behaviours for one requirement, and Example 6's `npm test` gate surfaces nothing on the Workflow path
- 020-args-events-model-is-declared-by-the-template-but-never-documented — applied 2026-08-28 (VERSION.json 1.3.0->1.4.0; 2/3 verified, floor clear; widened to all three args.events shapes) — template L79-90 declares `args.events.model` and asserts "the commander passes an id it has verified exists"; SKILL.md's three `args.events` shapes (L353, L607, L290-292) never mention it, so `EVENT_MODEL` is dead and every per-transition emitter spawns on the session model
- 019-verification-cli-steps-fan-out-into-worktrees-that-cannot-build — applied 2026-08-28 (VERSION.json 1.3.0->1.4.0; 3/3 verified, floor clear; ONE edit, exempt-not-serialize; companion filed as sk-sequence-planner 005) — SKILL.md L177-179 endorses fanning `npm test`/`lint`/`build` across services and L165-168 turns that into `isolation: 'worktree'` members, which CLAUDE.md's "never install or build inside a worktree" hard rule forbids; `node_modules`/`link-deps`/`npm install` have ZERO occurrences in the whole skill dir
- 018-work-item-and-stage-count-are-unbound-shell-vars — applied 2026-08-28 (VERSION.json 1.3.0->1.4.0; 2/3 verified, floor clear; step renumbering NOT applied) — `$WORK_ITEM` is used at SKILL.md L325 and L346 and assigned nowhere in the skill (grep: those two lines only), and `stages=<n>` is never resolved; the sidecar is opened in run-procedure step 1, before the step-2 parse that is the only source of either value, so every run lands in `_adhoc/sk-commander`
- 017-integrate-misdiagnoses-a-stale-base-conflict — proposed 2026-08-18 (raised during verification, NOT yet refuter-verified) — `integrate` asserts "disjoint subtrees ⇒ no conflict" and halts with "wave was not independent"; the recorded convention says the harness can seed a worktree from a stale base, so a correct wave gets halted and blamed. Diagnose the base before attributing the cause
- 016-cross-scope-worktrees-are-created-without-consent — REJECTED 2026-08-18 (0/3; premise superseded by the operator's confirmed branch-autonomy standing grant) — split out of the original 011. Cross-scope waves create worktrees with no ask and no hook can see a harness worktree. The observation holds — no hook can see a harness worktree — but the gate needs a presence signal that does not exist, and the operator's standing grant withdraws the ask entirely
- 015-manifest-comment-cites-the-wrong-skill-md-line — proposed 2026-08-18 (0/3 verified) — skill.manifest.yaml L31 says "documented, seen at SKILL.md:405" but L405 is the Worktree addendum; the baseline sha256 matches, so the citation was wrong when written — replace with a heading anchor
- 014-skill-md-is-714-lines-with-two-conditional-blocks-inline — proposed 2026-08-18 (0/3 verified) — 714-line always-loaded SKILL.md carries ~265 lines only some runs need (Fallback 90, Examples 83, ralph 46, Jira 46); move to references/, Fallback first since its duplicated briefs already drifted (013)
- 013-fallback-briefs-have-drifted-from-the-template — proposed 2026-08-18 (1/3 verified, floor clear) — the Fallback's hand-copied briefs lost the template's timestamp instruction and its model pin (so the Run summary's Started/Completed columns are never collected), and the Jira addendum paraphrases the repo-root walk-up L440 spells out literally
- 012-fable-resolver-diagnosis-is-claimed-but-not-implemented — applied 2026-08-18 (VERSION.json 1.1.0→1.2.0; 3/3 verified, floor clear; option (b) only) — the "template already encodes, don't re-derive" list promises a bounded sk-fable-resolver halt diagnosis; `fable` appears once in the whole skill (the promise itself). Fix (b) only — option (a) is dead, `agentType` does not exist in the template's agent() opts
- 011-integrate-cleanup-is-wider-than-the-merge — REJECTED 2026-08-18 (1/3; diagnosis CONFIRMED but items 1/4 have no data path — integrate returns only a boolean) — `integrate` is handed only the merged branches but tells the agent to remove "each commander worktree", inviting a `--force`; and the Workflow path never cites the leftover the Fallback path is required to cite
- 010-preflight-has-no-protected-branch-check — REJECTED 2026-08-18 (0/3; trigger undecidable at pre-flight, ~66 of ~106 sequences would halt — re-filed as docs/implementation-plans/branch-safety-commit-class/proposal.md) — the pre-flight has no branch check although CLAUDE.md's rule names "commander sequences" and the hook has no `git commit` class at all. The narrowed trigger turned out to be undecidable at pre-flight and its `artifacts/runs/` carve-out is void in shp-sk, where those files are tracked
- 009-model-tier-token-is-undefined — REJECTED 2026-08-18 (0/3; tier grammar one value short, and it is round three of a churn cycle — planner artifact 004 went the opposite way, 3-0 verified) — the parse spec defines `model=<id>` but glosses it "e.g. `model_tier=mid`", the Examples use only `model_tier=`, the template has 0 occurrences. Superseded counts: 103 sequence files, not 98; 0 carry the token. Refuted because the fix's tier grammar omits `top` and because sk-sequence-planner owns this vocabulary
- 008-service-add-scaffolding-is-wrong-three-ways — applied 2026-08-18 (VERSION.json 1.1.0→1.2.0; 3/3 verified, floor clear) — Example 6 and the template's "REAL CLI signature" comment both use `service add <git-url> <name>`; the real verb is `add <name> [<git-url>]`, so the step hard-fails NAME_PATTERN and halts the run, and `add` never creates `src/` — Example 6 also omits the required `service pull`. Both follow-ups CLOSED: memory in 3b9318a0 (commander-cli-steps corrected + the signature registered as an `implementation` rule), planner and the live architecture docs in 04a4e4f7
- 007-resolve-anchor-main-module-guard-not-portable — applied 2026-08-18 (VERSION.json 1.1.0→1.2.0; 3/3 verified, floor clear) — resolve-anchor.mjs gates its CLI on `import.meta.url === `file://${process.argv[1]}``, false through the `.claude/skills` exposure symlink TODAY plus on spaced paths and on Windows → Step 3a bakes an EMPTY work_dir silently. Fix is `realpathSync` (measured: the only form true across all four invocation shapes) + a halt-on-empty
- 006-add-triggering-eval-set — applied 2026-08-28 (2/3 verified on re-verification, floor clear; the 0/3 of 2026-08-18 recorded no reason and no votes were ever cast; prod path in query 1 swapped for a fictional one; manifest re-recorded) — sk-commander has no evals/ dir at all, so `auditor.sh score` exits 4 and rubric dimension 1 (description triggering) has never been measured; add a 20-query trigger-eval.json (10 should-trigger / 10 neighbour near-misses)
- 005-leading-dot-definition-omits-bare-dotdot — applied 2026-06-26 (VERSION.json 1.0.1→1.1.0, batch AAP-1) — added bare `..` to the file-relative anchor definition (now "`.` or `..`, or begins with `./` or `../`"), aligning the prose with the bundled resolve-anchor.mjs isLeadingDot [jira:AAP-25]
- 004-step3a-extract-embedded-anchor-before-helper — applied 2026-06-26 (VERSION.json 1.0.1→1.1.0, batch AAP-1) — rewrote Step 3a to split BARE vs EMBEDDED anchors: for `docs_dir=`/`artifacts_dir=` tokens the model must EXTRACT the value, pass only it to the helper, then splice the resolved path back (helper doesn't parse tokens). Prose-only; resolve-anchor.mjs untouched [jira:AAP-24]
- 003-set-remote-is-registry-mutating — applied 2026-06-26 (VERSION.json 1.0.1→1.1.0, batch AAP-1) — added `project set-remote` to the same list (`project create/add/remove/use` → `…/set-remote`), so the pre-flight catches two set-remotes in one wave (race the root index + root-repo .gitmodules) [jira:AAP-23]
- 002-service-pull-is-registry-mutating — applied 2026-06-26 (VERSION.json 1.0.1→1.1.0, batch AAP-1) — added `service pull` to the scope-race registry-mutating verb list (`service add/remove/use` → `service add/pull/remove/use`), so the Step-3 pre-flight catches two pulls in one wave; aligns with the paired planner + service.mjs [jira:AAP-22]
- 001-ralph-arm-guard-when-plugin-absent — applied 2026-06-19 (VERSION.json 1.0.0→1.0.1, auto-verified 2/3) — ralph-arm exemplar now wraps the arm in an if/elif/else guarding plugin-absent (run normally) + already-armed (don't double-arm), matching the prose
