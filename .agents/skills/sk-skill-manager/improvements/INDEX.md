# Improvements — sk-skill-manager

One line per artifact: id — status — hook.

- 001-export-destination-registry — applied 2026-08-14 — no source-side record of what was exported where; multi-destination `skill_repo`, a derived `skill destinations` verb, and per-skill destination intent in `skill.yaml`
- 002-export-picklist-selection — applied 2026-08-14 — REVIEW asked "which to export" with no mechanism; `scripts/export-picklist.mjs` (`plan` writes a tickable pick list, `resolve` turns the ticks into export commands) and a rewritten REVIEW
- 003-discover-by-intent — applied 2026-08-14 — every path to an uninstalled skill required knowing its NAME; `scripts/skill-search.mjs` (`index` + `find "<intent>"`), a DISCOVER mode, and a description trimmed 1161 -> 1013 chars to fit the triggers under the 1024 cap
- 004-trigger-eval-set — applied 2026-08-28 — no committed triggering benchmark, so rubric dimension 1 is unmeasurable (`auditor.sh score` exits 4); adds `evals/trigger-eval.json`, 20 queries, 10 should-trigger (ten of the sixteen modes — see 017) + 10 sibling near-misses
- 005-broken-guide-links — applied 2026-08-28 — seven `../../../docs/guide/*.md` links across four documents do not resolve (SKILL.md:36,122,184,864,866,868,870), including the "read the structure guide" pointer; repoint at their real `pending-update/` and `v1.5/` paths
- 006-stale-manifest-check-claim — applied 2026-08-28 — SKILL.md:225-226 asserts `skill manifest --all --check` fails on "eight vendor skills pending create"; live run shows five first-party skills all pending `update`, and the set moved within one audit run — keep the durable advice, drop the frozen count
- 007-offload-routing-contradiction — applied 2026-08-28 — description says "NOT for offloading (sk-skill-offload)" while SKILL.md:76 routes "park X"/"archive X" to a local OFFLOAD mode (SKILL.md:673-683) whose CLI verb sk-skill-offload already runs itself; make the body hand off, keep the OFFLOAD-vs-REMOVE distinction
- 008-import-backup-path — applied 2026-08-28 — IMPORT says `--force` backups land under `$ARTIFACTSDIR/backups/` (SKILL.md:596) while REMOVE says `artifacts/runs/skill-manager/backups/` (:707); `_shared.mjs:123` hardcodes the latter and reads no `artifacts_dir` — also falsifies the "never hand-joined" claim at :44
- 009-architect-pre-v2-runbase — applied 2026-08-28 — ARCHITECT checks 2-3 grade against the pre-v2 `artifacts/runs/<skill-id>/` shape (SKILL.md:192-196), contradicting the file's own v2 section at :57-59; it would fail a correct v2 skill and pass a pre-v2 hand-join
- 010-not-carried-append-unwired — applied 2026-08-28 — `assets/not-carried-withholding.md` has zero live references and SKILL.md:307 omits the `--append` flag built to carry it; the published NOT-CARRIED.md has 0 "Withheld" hits, the exact durability trap the flag exists to escape
- 011-help-not-authoritative — applied 2026-08-28 — SKILL.md:39-40 forbids hardcoded flag lists and calls `--help` the real surface, but the file carries 9 rosters and `--help` omits the `--category` that :350 makes mandatory, routing an agent to the forbidden `--preset`
- 012-progressive-disclosure-bootstrap-settings — PARKED (safety floor, awaiting a human) — 872 lines; BOOTSTRAP's 64-line once-per-destination procedure and the ~56-line settings-vs-configuration restatement of AGENTS.md move to `references/`, keeping the routing table, runs-layout section, three "failures to refuse" and both BOOTSTRAP guards
- 013-description-trim-and-offload-boundary — applied 2026-08-28 — MEASURED: incumbent 19/20 twice, sole failure `park sk-office-viz` over-triggering at rate 1.00; winner 20/20 twice at 1018 chars, returning the description under the 1024 cap it was 107 over. Replication bar 20>19 cleared
- 014-repo-layout-doc-stale-at-layout-3 — applied 2026-08-28 — `references/skill-repo-layout.md` titles itself "layouts 1 and 2" and says "Layout 2 is what export writes today", while its own §3b/§3c say layout 3 superseded it on 2026-08-18 and the live checkout's `.sidekicks/` matches layout 3; TOC has no §3c entry
- 015-repo-layout-doc-dead-source-repo-url — applied 2026-08-28 — `references/skill-repo-layout.md:121` still cites the retired `sidekicks-framework` URL (404s since the 2026-08-17 rename) that `config.defaults.yaml` and the published catalog both replaced with `sidekicks-harness`
- 016-require-clean-bundle-inert — applied 2026-08-28 — `config.defaults.yaml:67` `require_clean_bundle` reads as a knob but nothing consumes it; the export staleness refusal is real (`export.mjs:300`) and unconditional, so setting it `false` is silently ignored
- 017-eval-set-mode-coverage — proposed — the eval set this run committed covers 10 of 16 modes; ARCHITECT and VALIDATE (the description's 2nd and 3rd clauses) have no query, so the next trim can drop a mode unmeasured
- 018-picklist-dirty-detection-dead — applied 2026-08-28 — `export-picklist.mjs:139` tests `.sidekicks`/`skills` while `SKILLS_REL` is `.agents/skills`, so `dirtySkills()` always returns empty and the documented **UNCOMMITTED** flag is dead code; proven live (shipped logic 0 dirty, fixed logic 1). Introduced by the canonical-tree rename `9c9654c1`
- 019-agent-tray-browses-under-ops — applied 2026-08-28 — `sk-agent-tray` audits under `ops` so it browses on the published ops page and is absent from `agents`; `categories.yaml` has the override mechanism (used for `framework`) and does not apply it here
