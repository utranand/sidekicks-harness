# skill.manifest.yaml — what this skill needs in order to run.
#
# Sections under `requires:` are AUTHORED: generated once from what the scanner found, then
# maintained by hand. `config:`, `framework_hooks:` and `bundle:` are DERIVED — they are
# regenerated by `sidekicks skill manifest <skill> --apply` and a hand edit is reported as
# drift. Every `TODO` below is a question only a human can answer; `sidekicks skill doctor`
# keeps asking until it is gone.
#
# Writing prose here: `&word` and `*word` are refused by the yaml-subset reader ANYWHERE in
# a line, quoting and block scalars included (lib/yaml-subset/yaml.mjs findPoison) — so
# spell "and", and emphasize without asterisks, or the file stops loading.
#
# Every quoted value must OPEN AND CLOSE ON ONE LINE — this subset has no multi-line scalars,
# and a wrapped value is refused outright rather than silently truncated.

schema: 1
skill: skill-nickname

requires:
  # Installed into the single repo-root .venv. `import:` is the name the code uses, so
  # verification can probe it without resolving pip metadata.
  python:
    - package: PyYAML
      import: yaml
      why: 'the skill IS a YAML lookup: resolve_nickname.py reads assets/nicknames.yaml (bundled) and fuzzy-matches a spoken nickname to a skill id with difflib. Guarded at resolve_nickname.py:22-27 with an explicit exit 2 naming the repo-root venv interpreter, because the failure is always the same one - the skill was invoked with a system python that has no PyYAML.'
      # seen at scripts/resolve_nickname.py:23
  # DERIVED from skill.yaml — do not hand-edit.
  config:
    block: skill_nickname

# DERIVED hash baseline (LF-normalized sha256) — do not hand-edit.
bundle:
  SKILL.md: sha256:4689c4ce37845a787104e73fa854f834cc9b836764d58f8fa3ebaba04d10b3a9
  VERSION.json: sha256:dc39b97c30aad9d0f5ff2546917815947a6d483275f76cc901afbdd558c71209
  assets/nicknames.yaml: sha256:07392000986552e54158678d53630dd24c7f829835270573a8eb22a0c13f42f5
  scripts/resolve_nickname.py: sha256:1cda90bcc642c714de98d2fd537124155739c79df7feadc8377bb675be2b6fff
  skill.yaml: sha256:b24972059a8b0e50f2dd0624ded12675528486865d3a5317919875decd0a01cb
