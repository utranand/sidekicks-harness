# skill.manifest.yaml — what this skill needs in order to run.
#
# Sections under `requires:` are AUTHORED: generated once from what the scanner found, then
# maintained by hand. `config:`, `framework_hooks:` and `bundle:` are DERIVED — they are
# regenerated by `sidekicks skill manifest <skill> --apply` and a hand edit is reported as
# drift. Every `TODO` below is a question only a human can answer; `sidekicks skill doctor`
# keeps asking until it is gone.
#
# Writing prose here: `&word` and `*word` are refused by the yaml-subset reader ANYWHERE in
# a line, quoting and block scalars included (lib/yaml-subset/yaml.mjs findPoison) — so
# spell "and", and emphasize without asterisks, or the file stops loading.

schema: 1
skill: sk-config-doctor

requires:
  # Repo-root files this skill runs or reads. They do NOT travel with the skill, so
  # `degraded:` has to say what a lifted copy loses.
  framework_files:
    - path: scripts/send-mail.py
      degraded: 'the mail_sender block cannot be probed. Every other block in the delegation table at SKILL.md:163-169 is verified by asking its OWNING connector skill for a test verb; mail_sender has no connector, so this skill runs the sender itself in build-only mode (--dry-run, never a send) as the only way to prove the values resolve. Diagnosis of the other four blocks is unaffected, and a config with a mail_sender block is still reported as present - only the value check is lost.'
      # seen at SKILL.md:169
      #
      # SECOND CONSUMER of the P5 move candidate recorded in sk-report's manifest. When
      # scripts/send-mail.py moves into that skill, this row becomes a sibling_skills edge on
      # sk-report rather than a framework_files row, and the degraded: text above stays true
      # word for word. The third reference is sk-hello, which only DOCUMENTS the path.

# DERIVED hash baseline (LF-normalized sha256) — do not hand-edit.
bundle:
  SKILL.md: sha256:198908336a007135039fa971f60708ebebc5dd30e779afd01b4e822b964ece46
  VERSION.json: sha256:86a579f1dc8a18d070e0e6a1f68488a04c94d19384c9f2c25888ce11f82fdf72
