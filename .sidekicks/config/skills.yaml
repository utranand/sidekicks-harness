# skills.yaml — the 'skills' configuration family (COMMITTED).
#
# CONFIGURATION, not settings: values. The boolean enable map lives in
# .sidekicks/config/settings/. See docs/guide/settings-vs-configuration.md.
#
# Credentials NEVER live here. Every credential key appears with an empty value so a fresh
# clone can see what to supply; the values go in the git-ignored 'skills.secret.yaml'
# sibling. Write with `sidekicks config set`, read with `sidekicks config get <block>`.
#
# A block that is present but entirely commented out is INERT: it documents the schema, and
# the block still resolves to the owning skill's config.defaults.yaml until a key is
# uncommented here.

##############################################################################
# Generated by `sidekicks config sync` from the installed skills' config.defaults.yaml.
# Uncomment a key to override the skill's default.
##############################################################################

# skill_manager:
#   skill_repo:
#     layout: 3
#     public:
#       remote: https://github.com/utranand/sidekicks-skills.git
#       checkout: projects/global/services/sidekicks-skills/src
#       source_repo: https://github.com/utranand/sidekicks-harness.git
#     private:
#       remote: https://github.com/utranand/sidekicks-skills-private.git
#       checkout: projects/global/services/sidekicks-skills-private/src
#       source_repo: https://github.com/utranand/sidekicks-harness.git
#   export:
#     with_deps: true
#     require_clean_bundle: true
#     source_repo: https://github.com/utranand/sidekicks-harness.git
#   import:
#     backup: true
#     allow_force: false
#   bmad:
#     repo: https://github.com/bmad-code-org/BMAD-METHOD.git
